# Ads toggle — set to "false" to hide all AdBanner / ResponsiveAd / SideRailAds output.
# Any other value (or unset) keeps ads enabled. Client-visible: must have NEXT_PUBLIC_ prefix.
NEXT_PUBLIC_ADS_ENABLED=true
